version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - npm install -g gatsby-cli
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - gatsby build
  post_build:
    commands:
      - aws s3 sync public s3://gatsby-src